---
NOTE: do not edit this file, it is generated in apps/svelte.dev/scripts/sync-docs/index.ts
title: Installation locale
---

La version locale (ou stdio) du serveur MCP est disponible via le paquet npm
[`@sveltejs/mcp`](https://www.npmjs.com/package/@sveltejs/mcp). Vous pouvez soit l'installer
globalement puis le référencer dans votre configuration, soit l'exécuter avec `npx` :

```bash
npx -y @sveltejs/mcp
```

Voici comment l'installer pour quelques uns des clients MCP classiques :

## Claude Code

Pour inclure la version locale du MCP dans Claude Code, lancer simplement la commande suivante :

```bash
claude mcp add -t stdio -s [scope] svelte -- npx -y @sveltejs/mcp
```

Le `[scope]` doit être `user`, `project` ou `local`.

## Claude Desktop

Dans la section Settings > Developer, cliquez sur Edit Config. Cela va ouvrir un dossier contenant
un fichier `claude_desktop_config.json`. Modifiez le fichier pour inclure la configuration suivante
:

```json
{
	"mcpServers": {
		"svelte": {
			"command": "npx",
			"args": ["-y", "@sveltejs/mcp"]
		}
	}
}
```

## Codex CLI

Ajoutez la configuration suivante dans votre fichier `config.toml` (qui se trouve par défaut dans
`~/.codex/config.toml`, mais référez-vous à la [documentation de
configuration](https://github.com/openai/codex/blob/main/docs/config.md) pour plus de détails) :

```toml
[mcp_servers.svelte]
command = "npx"
args = ["-y", "@sveltejs/mcp"]
```

## Gemini CLI

Pour inclure la version locale du MCP dans le CLI de Gemini, lancer simplement la commande suivante
:

```bash
gemini mcp add -t stdio -s [scope] svelte npx -y @sveltejs/mcp
```

Le `[scope]` doit être `user`, `project` ou `local`.

## OpenCode

Lancez la commande :

```bash
opencode mcp add
```

et suivez les instructions, en sélectionnant "Local" dans le prompt "Select MCP server type" :

```bash
opencode mcp add

┌  Add MCP server
│
◇  Enter MCP server name
│  svelte
│
◇  Select MCP server type
│  Local
│
◆  Enter command to run
│  npx -y @sveltejs/mcp
```

## VS Code

- Ouvrez la palette de commande
- Sélectionnez "MCP: Add Server..."
- Sélectionnez "Command (stdio)"
- Entrez `npx -y @sveltejs/mcp` dans l'input et appuyez sur `Entrée`
- Lorsque l'on vous demande un nom, précisez `svelte`
- Choisissez si vous souhaitez l'ajouter en tant que serveur MCP `Global` ou `Workspace`

## Cursor

- Ouvrez la palette de commande
- Sélectionnez "View: Open MCP Settings"
- Cliquez sur "Add custom MCP"

Cela ouvrira un fichier contenant vos serveurs MCP, à l'intérieur duquel vous pouvez ajouter la
configuration suivante :

```json
{
	"mcpServers": {
		"svelte": {
			"command": "npx",
			"args": ["-y", "@sveltejs/mcp"]
		}
	}
}
```

## Zed

Installez l'extension [Svelte MCP Server extension](https://zed.dev/extensions/svelte-mcp).

<details>

<summary>Configure Manually</summary>

- Ouvrez la palette de commande
- Cherchez et sélectionnez "agent: open settings"
- Dans les paramètres, cherchez l'option `Model Context Protocol (MCP) Servers`
- Cliquez sur "Add Server"
- Sélectionnez "Add Custom Server"



Cela ouvrira une popup avec la configuration du serveur MCP où vous pourrez ajouter la configuration
suivante :

```json
{
	"svelte": {
		"command": "npx",
		"args": ["-y", "@sveltejs/mcp"]
	}
}
```

</details>

## Autres clients [!VO]Other clients

Si nous n'avons pas inclus le client MCP que vous utilisez, merci de vous référer à leur
documentation concernant les serveur `stdio` et utilisez `npx` comme commande et `-y @sveltejs/mcp`
comme arguments.
