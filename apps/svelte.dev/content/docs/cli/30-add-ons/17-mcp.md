---
NOTE: do not edit this file, it is generated in apps/svelte.dev/scripts/sync-docs/index.ts
title: mcp
---

[Le MCP de Svelte](/docs/ai/overview) peut aider votre LLM à écrire du meilleur code Svelte.

## Usage

```sh
npx sv add mcp
```

## Ce que vous obtenez [!VO]What you get

- Une configuration MCP pour une installation [locale](/docs/ai/local-setup) ou
[distante](/docs/ai/remote-setup)
- Un fichier [README pour agents](https://agents.md/) pour vous aider à utiliser le serveur MCP de
manière efficace

## Options

### ide

L'IDE que vous voulez utiliser comme `'claude-code'`, `'cursor'`, `'gemini'`, `'opencode'`,
`'vscode'`, `'other'`.

```sh
npx sv add mcp="ide:cursor,vscode"
```

### setup

L'installation que vous souhaitez utiliser.

```sh
npx sv add mcp="setup:local"
```
